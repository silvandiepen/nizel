name: Publish

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      - name: Generate release notes
        run: node scripts/generate-release-notes.mjs

      - name: Commit release notes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add documentation/release-notes/
          git diff --cached --quiet || git commit -m "docs: generate release notes"
          git push

      - name: Publish nizel
        run: npm publish --workspace nizel --access public --provenance || echo "::warning::nizel publish skipped (version may already exist)"

      - name: Publish plugin-autolink
        run: npm publish --workspace nizel-plugin-autolink --access public --provenance || echo "::warning::plugin-autolink publish skipped (version may already exist)"

      - name: Publish plugin-code-copy
        run: npm publish --workspace nizel-plugin-code-copy --access public --provenance || echo "::warning::plugin-code-copy publish skipped (version may already exist)"

      - name: Publish plugin-shiki
        run: npm publish --workspace nizel-plugin-shiki --access public --provenance || echo "::warning::plugin-shiki publish skipped (version may already exist)"

      - name: Publish plugin-alert
        run: npm publish --workspace nizel-plugin-alert --access public --provenance || echo "::warning::plugin-alert publish skipped (version may already exist)"

      - name: Publish plugin-deflist
        run: npm publish --workspace nizel-plugin-deflist --access public --provenance || echo "::warning::plugin-deflist publish skipped (version may already exist)"

      - name: Publish plugin-emoji
        run: npm publish --workspace nizel-plugin-emoji --access public --provenance || echo "::warning::plugin-emoji publish skipped (version may already exist)"
