# Large Fixture

This document repeats a representative section several times. It is intentionally boring because parser benchmarks should spend their time parsing Markdown, not surprising the reader.

## Section 1

Markdown processors need to handle paragraphs, links to [documentation](https://example.com/docs), escaped characters like `*`, and nested structures.

- Alpha item with **strong text**
- Beta item with `inlineCode()`
- Gamma item with [a reference][docs]

> A blockquote with multiple lines.
> It includes *emphasis* and a nested list:
>
> - Nested one
> - Nested two

```js
export const value = 1;
export const double = (input) => input * 2;
```

## Section 2

Markdown processors need to handle paragraphs, links to [documentation](https://example.com/docs), escaped characters like `*`, and nested structures.

- Alpha item with **strong text**
- Beta item with `inlineCode()`
- Gamma item with [a reference][docs]

> A blockquote with multiple lines.
> It includes *emphasis* and a nested list:
>
> - Nested one
> - Nested two

```js
export const value = 2;
export const double = (input) => input * 2;
```

## Section 3

Markdown processors need to handle paragraphs, links to [documentation](https://example.com/docs), escaped characters like `*`, and nested structures.

- Alpha item with **strong text**
- Beta item with `inlineCode()`
- Gamma item with [a reference][docs]

> A blockquote with multiple lines.
> It includes *emphasis* and a nested list:
>
> - Nested one
> - Nested two

```js
export const value = 3;
export const double = (input) => input * 2;
```

## Section 4

Markdown processors need to handle paragraphs, links to [documentation](https://example.com/docs), escaped characters like `*`, and nested structures.

- Alpha item with **strong text**
- Beta item with `inlineCode()`
- Gamma item with [a reference][docs]

> A blockquote with multiple lines.
> It includes *emphasis* and a nested list:
>
> - Nested one
> - Nested two

```js
export const value = 4;
export const double = (input) => input * 2;
```

[docs]: https://example.com/reference

